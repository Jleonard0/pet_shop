<main class="container text-start ">
    <div class="row justify-content-start align-items-start" style="min-height: 80vh;">
        <div class="row justify-content-start">
            <h2>Adicionar serviço</h2>
            <p class="text-secondary">Você pode adicionar serviços a tipo de funcionario.</p>
            <form action="/funcionalidade/adicionar_servico" method="post" class="text-start col-md-6">
                <label for="name" class="form-label m-1">Nome</label>
                <input type="text" id="name" name="name" class="form-control m-1" required>
                <label for="value" class="form-label m-1">Preço</label>
                <input type="number" step="0.10" id="value" name="value" class="form-control m-1" required>
                <label for="role" class="form-label m-1">Tipo de funcionario</label>
                <select id="RoleId" name="RoleId" class="form-select form-select-lg mb-3"
                    aria-label="Selecione qual a função do funcionario." required>
                    {{#each allRoles}}
                    <option value="{{this.id}}">{{this.name}}</option>
                    {{/each}}
                </select>
                <div class="d-flex justify-content-between my-1">
                    <button type="submit" class="btn btn-primary my-2">Adicionar serviço</button>
                </div>
            </form>
        </div>
    </div>
</main>